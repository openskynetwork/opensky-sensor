Schnittstellen:

-- Zur Library --
- configure(opensky config struct):
  - puffer: mit historie, maximum
  - Berechtigung: nur ext squitter, nur long, long & short, ac, fec, crc
- status(opensky status struct):
  - synchronisationsstatus gps
- frame(opensky frame struct):
- start: zuvor muss configure aufgerufen worden sein
- stop

-- Von Library --
- setzen von frame filtern auf fpga


Fragen:
- Werden auch kaputte Frames weitergegeben, wenn der User CRC/FEC abgeschaltet hat?
- Wird jeder String in ostream mit newline abgeschlossen, oder muss std::endl selbst angehängt werden?

